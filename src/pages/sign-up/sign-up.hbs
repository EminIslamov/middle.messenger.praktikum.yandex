<div class="container">
    <form class="login-form">
        <div class="inputs-wrapper">
            {{> Input label="Введите имя" name="first_name" }}
            {{> Input label="Введите фамилию" name="second_name" }}
            {{> Input label="Введите логин" name="login" }}
            {{> Input label="Введите почту" name="email" }}
            {{> Input label="Введите пароль" name="password" }}
            {{> Input label="Введите номер телефона" name="phone" }}
        </div>

        <div class="btns-wrapper">

            {{> Button label="Зарегистрироваться" type="primary" }}
            {{> Button label="Перейти к авторизации" type="link" page="login" }}
        </div>
    </form>
</div>
